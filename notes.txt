usar str.indexof(searchvalue) para checkar se um pedaço de texto existe em outro

!isNaN(parseInt('0')) // para verificar se é um número

.style para mudar o css

for (i of object) {}

achar % = porcentagem / valor total * 100

split quebra uma string em varias strings
reverse inverte um array
join transforma um array em string

document.querySelector() // seleciona o primeiro match

element.cloneNode(true) //copia um elemento e seus filhos

document.body.append // insere depois do ultimo filho

append aceita strings, appendChild apenas objects

classList.contains() // verifica se o elemento tem essa classe
classList.remove()
classList.add()

parseInt(
parseInt(var, 16) pode ser usado para checar se é hexadec

Funções de alta ordem: são funções que recebem outras funções como parametro, ou que retornam funções.

Array.from transforma nodelist em array

